OPENAI_API_KEY=sk-your-api-key-here

# Optional: custom OpenAI-compatible endpoint
# OPENAI_BASE_URL=https://your-custom-endpoint.example.com/v1

# Optional: override the model used by the tester
# Defaults to gpt-5-nano. For reasoning models like gpt-5.1 you can
# also control reasoning effort (the tester will set effort=low by default).
# OPENAI_MODEL=gpt-5.1

# Optional: development-only TLS bypass for self-signed endpoints or
# environments with SSL interception. DO NOT use in production.
# Set to "true" to disable TLS certificate verification.
# OPENAI_INSECURE_SKIP_VERIFY=false
